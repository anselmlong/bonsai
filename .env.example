OPENAI_API_KEY=sk-...
TAVILY_API_KEY=tvly-...
BRAVE_API_KEY=        # optional — fallback if Tavily fails
SERPER_API_KEY=       # optional — primary search (fallback chain: Serper → Tavily → Brave)
LANGSMITH_API_KEY=        # optional — enables tracing
